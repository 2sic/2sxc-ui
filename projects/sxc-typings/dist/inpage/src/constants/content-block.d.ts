/**
 * This just holds some constants, but it looks like _LayoutElement is unused - but I think it should be!
 * @internal
 */
export declare class ContentBlock {
    static cViewWithoutContent: string;
    static UseExistingTemplate: number;
}
/**
 * ContentBlock constants
 * @internal
 */
export declare const ContentBlockIds: {
    classes: {
        name: string;
    };
    selectors: {
        ofName: string;
    };
};
