/**
 * A log entry item
 * @export
 * @interface LogEntry
 */
export declare class LogEntry {
    message: string;
    /** A timestamp for this entry to better see sequences of things happening */
    time: number;
}
