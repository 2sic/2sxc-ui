import { ToolbarConfig } from '../../toolbar/toolbar/toolbar-config';
import { ContextBundleItem } from '../bundles/context-bundle-item';

export class ContextOfToolbar extends ContextBundleItem {
  toolbar: ToolbarConfig;
}
