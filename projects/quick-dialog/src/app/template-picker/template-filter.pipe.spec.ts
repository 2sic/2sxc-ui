import { TemplateFilterPipe } from './template-filter.pipe';

describe('templatePipe', () => {
  it('create an instance', () => {
    const pipe = new TemplateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
