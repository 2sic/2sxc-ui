import { ContentTypeFilterPipe } from './content-type-filter.pipe';

describe('ContentTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
