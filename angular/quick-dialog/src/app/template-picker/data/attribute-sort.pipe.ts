// import { Pipe, PipeTransform } from '@angular/core';
// import { log as parentLog } from 'app/core/log';

// const log = parentLog.subLog('pipe-ctsort');

// @Pipe({
//   name: 'attributeSort'
// })
// export class ContentTypeSortPipe implements PipeTransform {

//   transform(list: any[], attribute: string): any {
//     log.add('contentTypeSort');
//     return list && list.sort((a, b) => ('' + a[attribute]).localeCompare(b[attribute]));
//   }

// }
