import { ContentTypeSortPipe } from './content-type-sort.pipe';

describe('ContentTypeSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentTypeSortPipe();
    expect(pipe).toBeTruthy();
  });
});
